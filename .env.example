# VoyageAI Configuration
# Get your API key from https://www.voyageai.com/
VOYAGE_API_KEY=your_voyage_api_key_here

# Optional: VoyageAI embedding model to use
# Available models: voyage-3.5-lite, voyage-3.5, voyage-3-large, etc.
# Default: voyage-3.5-lite
VOYAGE_MODEL=voyage-3.5-lite

# LanceDB Storage Configuration
# For local storage (default):
LANCEDB_URI=./lancedb

# For AWS S3:
# LANCEDB_URI=s3://my-bucket/cache-data

# For Cloudflare R2 (S3-compatible):
# LANCEDB_URI=s3://my-r2-bucket/cache-data

# Optional: Cache table name
# Default: semantic_cache
CACHE_TABLE_NAME=semantic_cache

# Optional: Namespace for cache isolation
# Useful for multi-tenant applications or separating dev/prod caches
# Default: (none)
# CACHE_NAMESPACE=production

# Optional: Minimum similarity threshold (0-1)
# Higher values = stricter matching (more similar required for cache hit)
# 1.0 = exact match only, 0.9 = high similarity, 0.75 = low similarity
# Default: 0.9
CACHE_MIN_PROXIMITY=0.9

# S3 / S3-Compatible Storage Configuration
# Required when using s3:// URIs in LANCEDB_URI

# AWS S3 Credentials
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key
# AWS_SESSION_TOKEN=your_session_token  # Optional
# AWS_REGION=us-east-1

# For Cloudflare R2:
# AWS_ACCESS_KEY_ID=your_r2_access_key_id
# AWS_SECRET_ACCESS_KEY=your_r2_secret_access_key
# AWS_REGION=auto  # R2 requires 'auto' as the region
# S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com

# For MinIO or other S3-compatible storage:
# AWS_ACCESS_KEY_ID=minioadmin
# AWS_SECRET_ACCESS_KEY=minioadmin
# AWS_REGION=us-east-1
# S3_ENDPOINT=http://localhost:9000
# ALLOW_HTTP=true  # Required for non-HTTPS endpoints
